# Run 92 — unblock the gate, and put the reason before the imperative

**Repository:** `/home/user/LinPRojectRadar`, branch `main`.
**Starting commit:** `56c432a797dbcc403250fac89c0e0257bd9f710f` ("Run 91 report"), clean tree,
`main == origin/main` verified by `git rev-parse HEAD origin/main` returning the same hash twice.
**No migration was added.** Migration head remains `0030_extraction_contract`.
**Python:** system `/usr/local/bin/python3`, 3.11.15. No repo venv; none created.

---

## 1. THE GATE RESULT — all fifteen blocker classes

The gate **RAN at this repository's head**, not in a clone. Command, from the repo root:

```
/usr/local/bin/python3 server/tools/build_run37_acceptance.py
```

Exit 0. `FREEZE GATE: 15 blockers evaluated, 4 BLOCKED -> FINAL_FREEZE_BLOCKED`.
Artefact: `research/freeze/run67_successor_freeze_gate.csv`, 15 rows, committed.

No fallback, no substitution, no relaxed blocker. Run 91's removal of `_gate = _fresh or
_committed` stands — line 231 of `test_run37_freeze_gate.py` still reads `_gate = _fresh`, and
the gate filename is still read from the generator's `GATE_FILE` constant. Verified, not assumed.

| # | Blocker class | Count | State | Real drift, or a fault in the check |
|---|---|---|---|---|
| B01 | dirty candidate identity | 7 | **BLOCKED** | **Real drift.** |
| B02 | population mismatch | 0 | PASS | — |
| B03 | controlled-stimulus mismatch | 0 | PASS | — |
| B04 | participant-sequence drift | 2 | **BLOCKED** | **Real drift.** |
| B05 | false defensibility statement | 0 | PASS | — |
| B06 | unexpected execution exception | 0 | PASS | — |
| B07 | Category-9 bypass | 0 | PASS | — |
| B08 | Category-10 authority violation | 0 | PASS | — |
| B09 | voting count is not exactly 2 | 0 | PASS | — |
| B10 | current taxonomy dual authority | 0 | PASS | — |
| B11 | package or predecessor mutation | 13 | **BLOCKED** | **Real drift.** |
| B12 | browser qualification failure | 0 | PASS | — |
| B13 | unresolved blocking Run-36 defect | 0 | PASS | — |
| B14 | unsupported final empirical-validation claim | 0 | PASS | — |
| B15 | candidate behaviour changed during the run | 1 | **BLOCKED** | **Real drift, and already authorised at Run 89.** |

### The four that blocked, each in full

**B01 — dirty candidate identity, count 7.** Evidence as written to the artefact: *"11
content-addressed digests recomputed from the tree and compared; digests that diverge from the
candidate identity: 7"*. I resolved which seven, by recomputing each group myself:

```
DIVERGES  registry_digest                 (2 members)
DIVERGES  taxonomy_authority_digest       (4 members)
matches   qualification_authority_digest  (5 members)
DIVERGES  participant_protocol_digest     (8 members)
matches   controlled_stimuli_digest       (2 members)
DIVERGES  simulation_authority_digest    (40 members)
DIVERGES  test_suite_identity           (206 members)
matches   browser_suite_identity          (2 members)
matches   final_lock_guard_digest         (1 members)
DIVERGES  evidence_provenance_digest      (2 members)
DIVERGES  service_roster_digest           (1 members)
```

**Real drift.** `run67_freeze_candidate_identity.json` describes the tree as Run 67 left it. Runs
69 through 92 have edited the registry, the taxonomy authority, the simulation layer, the
participant surfaces and the test suites since. The check is doing exactly its job: it is
reporting that the tree has moved past the candidate identity it is measured against. This is not
a fault in the check and there is nothing to fix in it.

**B04 — participant-sequence drift, count 2.** Evidence as written:

> 5 sequence-bearing files compared against the og-participant-2026.08-v26 record; moved:
> `['assets/js/decision.js', 'assets/js/workspace.js']`; set shortened from 6 to 5 by the named
> exception `['assets/js/deepdive.js']`, each proved absent and declared in
> `V20_TO_V21_DELETED`: yes

**Real drift.** These are precisely the two files the drift record is about, and their movement is
measured, not asserted. **Writing the drift record did not and could not clear this blocker.** See
section 6, false premise 3 — that is the most important finding of this run.

**B11 — package or predecessor mutation, count 13.** Evidence as written:

> rewritten predecessor package records: none; og-participant-2026.08-v26 files not matching their
> record: `['assets/css/radar.css', 'assets/js/categories.js', 'assets/js/decision.js',
> 'assets/js/detail.js', 'assets/js/ingest.js', 'assets/js/knowledge.js',
> 'assets/js/neural_flow.js', 'assets/js/projectnet2d.js', 'assets/js/signals.js',
> 'assets/js/taxonomy.js', 'assets/js/workspace.js', 'index.html']`; live stamp `sim-2026.08-v43`
> (expected `sim-2026.08-v42`); predecessor `5c023bc97372` still stamped `sim-2026.08-v41`: True

**Real drift**, and the count decomposes exactly: 12 moved files + 1 for the version stamp
(`SIMULATION_VERSION` is `sim-2026.08-v43`; the generator's `EXPECTED_VERSION` is
`sim-2026.08-v42`) = 13. **No predecessor record was rewritten** — that half of B11 is clean, which
is the half that matters most, since it is the property that keeps every earlier package
reconstructable.

**B15 — candidate behaviour changed during the run, count 1.** Evidence as written:

> behaviour digest moved:
> `8fb4d3663fd3ee421814521b5b89257d90524eaf5ffba9018ebd19a9bb3dd7a1` ->
> `3839c63df2a02099e2d536defb4b89e07d55d1eeb5ac6fee31704f2307eacdb7`

**Real drift, and already authorised at Run 89.** The digest is `3839c63d…`, which is the value
Run 89 moved it to and which the briefing records. **It did not move again in this run** — Run 92
changed nothing that any module computes, and I measured that rather than assuming it. B15 remains
red only because the *record* it compares against is still the pre-Run-89 `8fb4d366…`; that record
is rewritten only when the gate is clean, which it is not.

### Why the gate is not green, stated plainly

All four standing blockers reduce to one fact: **the freeze candidate of record is Run 67's
(`v42`), and the tree is twenty-five runs past it.** B01, B04 and B11 are three different windows
onto that same gap, and B15 is the behaviour half of it. **Not one of them is a fault in a check.**
The gate is working correctly and telling the truth.

**The successor package was NOT minted.** §2.3 conditions minting on the gate returning clean. It
did not. I left it, and I did not relax anything to make it appear otherwise.

---

## 2. THE DRIFT RECORD, AS WRITTEN, IN FULL

**Location, established from the tooling itself and not from a filename.** The briefing's proposed
path `server/app/participant_packages.py` **does not exist**. The real module is
`server/tools/participant_packages.py`. Its convention is a
`V<N>_TO_V<N+1>_SEQUENCE_EXCEPTION` constant alongside `_CHANGED` and `_DELETED`, and it carries
two precedents of exactly this case — `V23_TO_V24_SEQUENCE_EXCEPTION` (Run 59, `decision-ui.js`)
and `V24_TO_V25_SEQUENCE_EXCEPTION` (Run 62, `workspace.js`), both content moves, both read in
full before writing. The v20→v21 constant was **not** used as the model: it is a *deletion* record
whose condition (b) requires the excepted file to be absent from the tree, and both files here are
present.

`PP.CURRENT` is `og-participant-2026.08-v26`, so the link is v26→v27.

**Hashes re-verified before anything was written, as §2.1 requires.** Run 91's sealed-blob recovery
reproduces exactly:

```
git show 7e7fc4e9:assets/js/decision.js  | sha256sum -> 4910660260217db893b1eec99ed309d05c7f7e6c65b3b75e124c84c16291cdc1
record  4910660260217db893b1eec99ed309d05c7f7e6c65b3b75e124c84c16291cdc1  assets/js/decision.js   MATCH

git show a8dfe094:assets/js/workspace.js | sha256sum -> a85fc707ddb8087fc58695388b1d06f5d734d14f183cc47fbfb069e779b9481c
record  a85fc707ddb8087fc58695388b1d06f5d734d14f183cc47fbfb069e779b9481c  assets/js/workspace.js  MATCH
```

And the five sequence-bearing files against the v26 record at this head:

```
MOVED      assets/js/decision.js         cur 8a0032ef…  rec 4910660260…
IDENTICAL  assets/js/decision-ui.js
MOVED      assets/js/workspace.js        cur fb64db3f…  rec a85fc707dd…
IDENTICAL  assets/questionnaires/intake.json
IDENTICAL  assets/questionnaires/debrief.json
```

Over all 69 members of the record: **12 moved, 0 absent**, which confirms the briefing's figure.

### The record as committed to `server/tools/participant_packages.py`

```python
#: RUN 92. NOTHING LEFT THE PACKAGE ACROSS THIS LINK. Declared as an EMPTY tuple rather than
#: omitted, so the identity builder reads a DECLARATION instead of falling back to a hard-coded
#: empty list. MEASURED: all sixty-nine members of the v26 record are present in the tree; none
#: is absent. A member disappearing without being named here still raises.
V26_TO_V27_DELETED: tuple[str, ...] = ()

#: RUN 92. The files whose bytes moved between v26 and the current tree. TWELVE, measured by
#: hashing every one of the sixty-nine members of code_audit/run63_participant_package_v26_
#: checksums.sha256 against the tree at 56c432a. TWO of the twelve are sequence-bearing.
V26_TO_V27_CHANGED = (
    "assets/css/radar.css",
    "assets/js/categories.js",
    "assets/js/decision.js",
    "assets/js/detail.js",
    "assets/js/ingest.js",
    "assets/js/knowledge.js",
    "assets/js/neural_flow.js",
    "assets/js/projectnet2d.js",
    "assets/js/signals.js",
    "assets/js/taxonomy.js",
    "assets/js/workspace.js",
    "index.html",
)

#: RUN 92. THE NAMED EXCEPTION OF RECORD FOR THIS LINK, declared here rather than discovered by
#: a checksum. Its authority is the owner's Run 92 order, section 2.1. Written by Lin Tun on
#: 2026-08-31. This is the record of a judgement the owner has made; it is not a request for
#: anyone's approval and nothing here awaits a countersignature.
#:
#: MEASURED, NOT ASSUMED. Of the five members of SEQUENCE_BEARING_FILES_FROM_V21, exactly two
#: moved. `assets/js/decision-ui.js`, `assets/questionnaires/intake.json` and
#: `assets/questionnaires/debrief.json` are present and byte for byte identical to the v26
#: record. The sealed CONTENT of the two that moved was recovered by walking every commit that
#: touched each path and taking the blob whose bytes hash to the sum the v26 record holds --
#: the record holds sums, not content -- and both recoveries were re-verified at this head:
#: `assets/js/decision.js` sealed at blob 4910660260217db8, the blob at commit 7e7fc4e9;
#: `assets/js/workspace.js` sealed at blob a85fc707ddb8087f, the blob at commit a8dfe094.
#:
#: WHAT MOVED INSIDE assets/js/decision.js, EXACTLY. Run 69 stopped the browser applying its own
#: red, amber and green thresholds to numbers the server had already stored. Those thresholds
#: existed nowhere else in the platform and nobody calibrated them. Each signal class now
#: displays the band the server actually stored, or nothing where the server stored none, and
#: the portfolio list says "not yet analysed" instead of a colour invented from indices. The
#: retired threshold function REMAINS IN THE FILE, unreferenced, carrying a comment that it must
#: not be wired back in.
#:
#: WHAT MOVED INSIDE assets/js/workspace.js, EXACTLY. Run 79 removed the Portfolio Health card
#: and its loader. Portfolio Health is retired -- portfolio_health.live_portfolio_modules()
#: returns nothing -- and the card's ENTIRE content was one sentence saying that no
#: portfolio-level reading is produced. The card carried NO controls. Three helpers that existed
#: only to word its rows went with it, and a per-project loader nothing else needed.
#:
#: THE JUDGEMENT, stated as a fact about the sequence. NO decision step, reveal gate, lock,
#: randomisation, questionnaire, append-only record or control was added, moved or removed.
#: What a participant is asked, in what order, with what options, and how it is recorded, is
#: UNCHANGED between the sealed version and its successor. Both changes are to what the
#: interface DISPLAYS, and both stopped the interface asserting something the analysis had not
#: established.
#:
#: THE SCOPE. This record covers exactly those two files at the content diffed above. It does
#: NOT authorise any future change to them, and it says NOTHING about the other ten changed
#: members of the v26 record, none of which is sequence-bearing.
#:
#: NOT ESTABLISHED, and therefore not recorded: participant counts, collection dates, and
#: anything about a live study population. No such figure was measured and none is asserted.
#:
#: HOW THIS RECORD IS AND IS NOT CONSUMED, measured at Run 92 from the generator's own code.
#: `build_run37_acceptance.py` blocker B04 reads only V20_TO_V21_SEQUENCE_EXCEPTION, which is
#: the DELETION path -- its condition (b) requires every excepted file to be ABSENT from the
#: tree. B04 otherwise hashes every member of the sequence-bearing set against PP.CURRENT's
#: record with NO content-move exception path, and B11 does the same over all sixty-nine
#: members. THIS CONSTANT THEREFORE DOES NOT AND CANNOT TURN B04 OR B11 GREEN. On the v23-to-v24
#: and v24-to-v25 links those blockers went green because a NEW checksum record was minted and
#: PP.CURRENT advanced to it; the exception constant was the declaratory half, read by
#: test_run28, test_run36 and the release builders, never by the blocker. No consumption path is
#: invented here.
V26_TO_V27_SEQUENCE_EXCEPTION = (
    "assets/js/decision.js",
    "assets/js/workspace.js",
)
```

**No signature block. No approval line. No blank date field.** The one structured attribution the
form asked for is filled with the owner's name and today's date, as §2.1 directs. Nothing in it
says or implies that anyone must countersign.

**Nothing was written into it that Run 91 did not establish.** Participant counts, collection
dates and anything about a live study population are recorded as *not established*, in those words.

---

## 3. `CANDIDATE`

| | |
|---|---|
| **Old value** (as it stood at `56c432a`, line 143) | `6ccb650cf8de57c8a09afb114dea0b6d70710368` |
| **Computed value at this run's head** | `eba2a11d6300dcb5e74bcc2b121bae55f4ebc407` |
| **What I set** | `eba2a11d6300dcb5e74bcc2b121bae55f4ebc407` |

The generator's own words for how it computed it: *"the oldest commit in the unbroken run back
from HEAD whose tree agrees with the working tree on all 266 member paths of
`run67_freeze_candidate_identity.json`"*.

`6ccb650c` is Run 89's commit "SIMULATION_VERSION moves to sim-2026.08-v43".
`eba2a11d` is Run 91's commit "Run 91 goal one: repair the freeze gate's two failing checks",
and I verified it **is an ancestor of HEAD in this repository** (`git merge-base --is-ancestor`).

**The generator did NOT still refuse after reconciling.** It ran to completion, wrote its four
artefacts including the fifteen-row gate, and returned `FINAL_FREEZE_BLOCKED` at exit 0.

I re-ran the generator again after goal two landed, at the final head, and it produced a
byte-identical gate artefact with `CANDIDATE as computed` still `eba2a11d…`. `assets/js/detail.js`,
which goal two changed, is **not** one of the 266 candidate-identity member paths, which is why the
constant did not move under it.

---

## 4. THE CHANGED BRIEF PASSAGE, IN FULL AS IT RENDERS

Rendered in Chromium (`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`) against a live
uvicorn port serving the real FastAPI app on a scratchpad copy of `server/dev.db`, via
`server/tools/drive_run91_brief.py`. **`PAGE ERRORS: []` at both widths.**
Brief bounding box 1189×1304 at 1280px, 943×1375 at 1024px.

**BEFORE** (Run 91's measurement, the imperative leading):

```
Escalate now, without waiting for an official posture: A4 Document-Derived Condition Signals reads
Red, set by A4.7 reading 3 open. What that reaches: A4 is a SUPPORTING category, so this reading
does not set the project status and could not have set it even had the required categories all
reported. What it does not reach: the required core, which is A1 assessed and A2, A3, A6
unassessed, so no project-level conclusion follows from it either way. What follows: treat this as
a category-level finding on A4 and act on it at that level; it is not evidence of a project-level
condition, and the absence of a project status is not evidence that there is none.
```

**AFTER**, copied verbatim from the rendered DOM at **1280px** — and byte-identical at **1024px**:

```
A4 Document-Derived Condition Signals reads Red, set by A4.7 reading 3 open. What that reaches: A4
is a SUPPORTING category, so this reading does not set the project status and could not have set
it even had the required categories all reported. What it does not reach: the required core, which
is A1 assessed and A2, A3, A6 unassessed, so no project-level conclusion follows from it either
way. What follows: Escalate now, without waiting for an official posture. Treat this as a
category-level finding on A4 and act on it at that level; it is not evidence of a project-level
condition, and the absence of a project status is not evidence that there is none.
```

The adverse reading is stated first. What it reaches and does not reach follows. The instruction
comes last, **verbatim in its wording and undiminished in its force** — only its position moved.

Every claim still names its figure: *"A4.7 reading 3 open"*. The served basis backing it, read
from the wire in the same run:

```
required_categories ['A1','A2','A3','A6']  supporting_categories ['A4','A5']
required_assessed ['A1']  required_missing ['A2','A3','A6']
supporting_assessed ['A4']  supporting_not_assessed ['A5']
fused_band Red  official false  status Indeterminate
```

Nothing else in the brief was rewritten. The whole-file diff is 12 lines changed in
`assets/js/detail.js`, seven of which are the explanatory comment.

---

## 5. PER GOAL — reached or not, every iteration including failures

### Goal one: the gate runs and returns a result — **REACHED**, in 2 iterations of a maximum 5

**Iteration 1 — measure the current state.** Ran the generator unmodified at head. It printed the
candidate fixed point and **refused at exit 3**: constant `6ccb650c…`, computed `eba2a11d…`. No
artefact was written; `git status --porcelain` was empty afterwards. *Not a failure — this is the
measurement the order asked for, and it also disproved a premise: the computed value did not
differ from Run 91's.*

**Iteration 2 — change one thing, then measure.** Wrote the drift record, set `CANDIDATE` to the
computed value, re-ran. **Goal met.** The generator ran to completion: 15 blockers evaluated, 4
BLOCKED, `FINAL_FREEZE_BLOCKED`, exit 0, gate artefact written with 15 rows.

I then re-ran once more after goal two landed to confirm the result holds at the final head. It
does, byte-identically. That was verification, not a further iteration.

**No failed iterations to report on goal one.** I did make one ordering choice worth stating: I
read B04's code *before* writing the record, which is what surfaced the finding that the record
cannot clear the blocker. Had I written first and read after, I would have reported a fix that was
not one.

### Goal two: the reason precedes the imperative — **REACHED**, in 1 iteration of a maximum 5

**Iteration 1.** Split the leading sentence: the category reading now stands on its own, and the
escalation moved into the "What follows" sentence. Updated Run 89's assertion (5). All four
sub-requirements met on the first measurement:

1. **Ordering** — reading, then what it reaches, then the instruction. Rendered above.
2. **Assertion (5) updated, not deleted, and proved able to fail** — see below.
3. **The three Run 70 checks unweakened and passing** — I did not touch them; the suite's section
   3 reports `[PASS] the production gate accepts the Indeterminate brief`, and its section 4
   non-vacuity probe still correctly rejects a figureless claim naming *"1. Every claim names the
   figure behind it"*. Every claim in the changed passage names its figure.
4. **Rendered in a real browser at 1280px and 1024px** — done, `PAGE ERRORS: []` at both.

**The non-vacuity proof, which §3.2 makes a hard gate.** Baseline snapshotted first. Then:

```
INJECT   removed "Escalate now, without waiting for an official posture. " from the What-follows push
RE-READ  grep -c "Escalate now, without waiting for an official posture" assets/js/detail.js  ->  0
RED      [FAIL] (5) escalates the assessed adverse conditions rather than waiting for the status,
                with the instruction AFTER the grounds rather than before them: got false, want true
         1 FAILURES
RESTORE  sha256 assets/js/detail.js -> 4f505e856bc97045d0f3b5ace4f7180ea5d0ed1a134abdd43d0f598d2bad3ae4
GREEN    ALL PASS
```

**The assertion is stricter after the change, not weaker.** It previously matched a literal string.
It now requires, for **both** A1 and A4 independently, that the reading, the grounds and the
imperative are all present **and that the imperative comes last** — plus a companion check
`(5-order)` asserting the old leading-imperative form is gone. A harness that only checked presence
would have stayed green through this edit; this one would not have.

`node server/tools/test_run89_indeterminate_brief.js` from the repo root: **15/15, ALL PASS.**

---

## 6. PREMISES IN THE ORDER AND BRIEFING THAT PROVED FALSE AGAINST THE TREE

**Five, and the third is the one that matters.**

**1. The drift record's location.** The briefing said the constant convention lives in
`server/app/participant_packages.py`. **That file does not exist.** The module is
`server/tools/participant_packages.py`. The convention itself was correct.

**2. §2.2.1: "Run 91 measured `eba2a11d…` in its own clone, but that was its own commit — the value
at this run's head will differ."** **It does not differ.** The generator computes
`eba2a11d6300dcb5e74bcc2b121bae55f4ebc407` at this head, the same value, and `eba2a11d` is an
ancestor of HEAD in *this* repository — Run 91's work was merged and pushed, so its commit is part
of `main`'s history, not a clone-local artefact. The order's instruction to recompute rather than
carry the number forward was right; the prediction about the outcome was wrong. I recomputed, and
report both values in section 3 as directed.

**3. THE MAJOR ONE — the premise that the drift record unblocks the gate.** §2 is titled "unblock
the gate" and orders the drift record as the first of three acts toward it. **The drift record
cannot unblock B04 or B11, and I established this from the generator's own code before writing it.**

`build_run37_acceptance.py`'s B04 consumes exactly one exception constant,
`V20_TO_V21_SEQUENCE_EXCEPTION`, and only on the deletion path — its condition (b) asserts *"every
excepted file is genuinely ABSENT from the tree — so the exception cannot be used to stop measuring
a file that is still there."* Outside that path B04 is:

```python
seq_moved = sorted(f for f in _seq_set
                   if hashlib.sha256((ROOT / f).read_bytes()).hexdigest() != current_pkg.get(f))
```

Every member hashed against `PP.CURRENT`'s record. **No content-move exception is read.** B11's
`current_bad` does the same across all 69 members, with no exception path whatsoever.

**What the two precedents actually did.** On v23→v24 and v24→v25, B04 and B11 went green because
each run **minted a new checksum record** — `run59_…_v24_checksums.sha256`,
`run62_…_v25_checksums.sha256` — and advanced `PP.CURRENT` to it. The blockers then compared the
tree against a record that described the tree. The `V<N>_TO_V<N+1>_SEQUENCE_EXCEPTION` constant was
the **declaratory** half of that act: it is read by `test_run28_participant_packages.py`,
`test_run36_fault_guards.py`, the `build_run*_successor_release.py` builders and the frozen-
immutability suites' authorised-change sets — **never by a blocker**. I verified this by grepping
every consumer of all three existing content-move constants.

**I did not invent a consumption path**, and I did not touch B04 to make one. The record is written
as the owner ordered and is correct and useful on its own terms — it is the standing declaration of
his judgement, and it is what a future mint will read. It simply is not the thing that turns the
gate green. **Minting the successor package is.** That was not authorised this run except
conditionally on a clean gate, so I left it.

**4. §2.2.3's implied premise, "if the generator still refuses after reconciling".** It did not
refuse. Reconciling `CANDIDATE` alone was sufficient at this head — Run 91's filename repair had
already removed the other obstacle. The briefing correctly flagged this as unmeasured and told me
to measure it rather than assume; I measured it, and the answer is that it now suffices.

**5. A briefing framing, minor.** The briefing warned to expect the tooling to rewrite sealed
records as a side effect and to check `git status --porcelain` after every invocation. I did, every
time. **There was no collateral rewriting in this run at all** — no `code_audit/*.csv`, no
`server/tools/run17/coverage.csv`, nothing under `research/freeze` beyond the gate's own artefact.
The three `code_audit/run37_*.csv` files were regenerated byte-identically and so never appeared as
modified. **The only file I committed that a tool wrote is
`research/freeze/run67_successor_freeze_gate.csv`, which is the gate's intended output.**

**Not false, and confirmed:** `V26_TO_V27_SEQUENCE_EXCEPTION` genuinely did not exist anywhere as a
declared symbol; the sealed record read by B04/B11 is
`code_audit/run63_participant_package_v26_checksums.sha256`; Run 91's two sealed-blob recoveries
reproduce exactly; the other three sequence-bearing files are byte-identical; 12 of 69 members
moved; the `__briefForTest` export keys are as briefed; the fallback stays removed.

---

## 7. REAL VERSUS HARNESS MEASUREMENTS, AND WHAT WAS NOT RUN

**There is no `ANTHROPIC_API_KEY` in this environment. Nothing in this report is the model's
behaviour, and nothing is presented as such.**

**Real measurements:**

- The gate run and all fifteen blocker states — the real generator, at this head, real tree bytes.
- Every SHA-256 in this report — computed from real file and git-blob bytes.
- The `CANDIDATE` computation — the generator's own `expected_candidate()` walking real git history.
- The brief render at 1280px and 1024px — real Chromium, real FastAPI app over a live port, text
  read back from the **rendered DOM**, not from a grep of the source.
- The non-vacuity proof — real file mutation, re-read from disk, real harness run.
- All suite results below.

**Harness / fixture, and NOT model behaviour:**

- The Indeterminate row's readings for A2, A4, A5, B1, B2 are a **fixture** stored directly by Run
  81's precedent path. The Red on A4 is a stored fixture value, not a model judgement.
- Document extraction throughout the driver runs under `StubExtractor`.
- `test_run89_indeterminate_brief.js` builds its own synthetic project payload in Node.

**Suites run, at the final head:**

| Suite | Result | Against baseline |
|---|---|---|
| `test_run89_indeterminate_brief.js` | **15/15 ALL PASS** | was 14/14; +1 check added |
| `test_run37_freeze_gate.py` | **27/34** | as Run 91 predicted: 4 real blockers + 3 consequences |
| `test_run36_fault_guards.py` | **41/41** | unchanged |
| `test_run28_participant_packages.py` | **355/360** | **identical at `56c432a`** — pre-existing, not caused here |
| `test_run38_frozen_immutability.py` | **10/17** | exactly the known pre-existing figure |
| `test_run39_frozen_immutability.py` | **13/19** | exactly the known pre-existing figure |
| `test_run22_production_tree_completeness.py` | **45/48** | exactly the known pre-existing figure |

The three suites the briefing warned might react to a declared exception — `test_run38`,
`test_run39` and `test_run36_fault_guards` — **did not move**. `test_run36` needed a clean tree
(`campaign_safety.CampaignTreeDirty`) and was run after committing.

**Note on a convention:** `test_run89_indeterminate_brief.js` terminates with `ALL PASS`, not the
canonical `RESULT: n/n checks passed` line. It is a Node suite and does not use the Python suites'
convention. I counted its 15 `[PASS]` lines directly rather than treating `ALL PASS` as sufficient.

**Not run, and why:** the eight document contracts, the cost investigation, Contractor Performance
Assessment's corpus path, the charts, and the retired specification archive — all excluded by §4.
The broader `test_documents_b7b`, `test_run3_adapter`, `test_run10_state_protection`,
`test_workspace_t3t5` and `test_run48_current_period` suites were **not run**; they are known
pre-existing failures on paths this run did not touch, and §4 puts their subject matter out of
scope.

---

## 8. FOUND AND NOT FIXED

1. **B04 and B11 have no content-move exception path.** Detailed in section 6, premise 3. Not
   fixed, because fixing it means either minting a successor package (not authorised on a blocked
   gate) or teaching B04 to honour a content-move exception — which would be **relaxing a blocker**
   and is exactly what §8.1 fails the run for. Flagged for the owner's decision.

2. **The freeze candidate of record is twenty-five runs stale.** `run67_freeze_candidate_identity.json`
   and `EXPECTED_VERSION = "sim-2026.08-v42"` both describe Run 67, while the tree stands at
   `sim-2026.08-v43`. This is the single root cause behind all four blockers. Advancing it is a
   mint, and a mint needs a clean gate.

3. **B15's comparison record is pre-Run-89.** The digest is at Run 89's authorised `3839c63d…` and
   did not move in this run, but the record it is compared against is still `8fb4d366…`. The
   generator only re-baselines when the gate is clean, so this cannot self-resolve while B01, B04
   and B11 stand. Correct behaviour; noted so it is not mistaken for new drift.

4. **`test_run28_participant_packages.py` is at 355/360.** Pre-existing — identical at `56c432a`
   before I touched anything. Not investigated; not caused by this run. Worth a future look.

5. **The A1 escalation ordering is proved only in the harness.** The browser fixture renders A4 as
   the sole adverse category, so the rendered proof of the new ordering covers A4. A1 is covered by
   assertion (5) in the Node harness, which tests both independently. Stated so it is not read as
   more than it is.

---

## 9. GUARANTEES — verified or not met

| Guarantee | State |
|---|---|
| `main == origin/main`, clean tree, repo and starting commit reported | **VERIFIED** |
| The gate evaluates all 15 blocker classes at this repository's head, not in a clone | **VERIFIED** |
| A result returned — `FINAL_FREEZE_BLOCKED`, exit 0 | **VERIFIED** |
| No fallback, substitution or relaxed blocker used to obtain any pass | **VERIFIED** |
| Run 91's removed fallback stays removed (`_gate = _fresh`, line 231) | **VERIFIED** |
| Gate filename still read from the generator's `GATE_FILE` | **VERIFIED** |
| The two sealed hashes re-verified before writing the record | **VERIFIED** |
| Drift record written where the tooling expects it, established from the tooling | **VERIFIED** |
| No signature block, approval line, or blank date in the record | **VERIFIED** |
| Nothing in the record that Run 91 did not establish; unknowns marked not established | **VERIFIED** |
| `CANDIDATE` recomputed at this head, old and new both printed, set and reported | **VERIFIED** |
| Run 89's assertion (5) updated, not deleted | **VERIFIED** |
| The updated assertion proved able to fail, then restored to green | **VERIFIED** |
| The three Run 70 recommendation checks unweakened and passing | **VERIFIED** |
| Every claim in the changed passage names its figure | **VERIFIED** |
| Brief rendered in real Chromium at 1280px and 1024px, `PAGE ERRORS: []` | **VERIFIED** |
| Changed passage printed in full as it renders | **VERIFIED** |
| Nothing in the brief rewritten beyond what goal two required | **VERIFIED** — 12 lines, 7 of them comment |
| No specification, `scope_signal_inputs`, Run 79/81 wiring, Conservative Dominance, Run 87 seam, or the three recommendation checks touched | **VERIFIED** |
| No migration added | **VERIFIED** |
| No `git add -A` or `git add .`; every add explicit; `git status --porcelain` before every commit | **VERIFIED** |
| `DATABASE_URL` never pointed at production Postgres — scratchpad SQLite copy only | **VERIFIED** |
| Five-iteration ceiling respected; no iteration hidden | **VERIFIED** — 2 on goal one, 1 on goal two |
| **The gate returns green** | **NOT MET, and deliberately so.** Four blockers stand, all real drift. §2.3 makes a result the goal, not a green result. |
| **The successor package minted** | **NOT MET.** §2.3 conditions it on a clean gate. Not authorised, not done. |
| A1's new ordering proved in a browser | **NOT VERIFIED IN THE BROWSER** — the fixture renders only A4 as adverse. Proved in the Node harness for both A1 and A4. Stated plainly rather than argued around. |

**Not pushed.** Ending state is reported below for verification.
