# Run 95 — the registry tells the truth; the decision card is STOPPED at the freeze boundary

**Repository:** `/home/user/LinPRojectRadar`, branch `main`.
**Starting commit:** `385cdb7` ("Run 94b report"), clean tree, `main == origin/main` verified.
**Interpreter:** system `python3` 3.11.15 at `/usr/local/bin/python3`. No repo venv; none created.
**NO MIGRATION WAS ADDED.** Head is unchanged at `0031_model_provider_attribution`.
**Stamp minted:** `SIMULATION_VERSION` `sim-2026.08-v43` → **`sim-2026.08-v44`**.
**Freeze gate: BLOCKED** at the CANDIDATE fixed-point refusal, before any blocker was evaluated.
Committed to `main`, **NOT pushed**, **not merged**, and the candidate constant was **not set**.

**GOALS ONE, TWO AND THREE ARE REACHED AND MEASURED. GOAL FOUR IS STOPPED UNDER §7.7** — the
Governance Decision card cannot be rebuilt without moving `assets/js/decision-ui.js`, which is
sequence-bearing. Nothing in that file was touched. Section 5 below states the collision.

---

## 1. The owner's deployment sequence

1. **No migration.** Nothing to run. The database is untouched by this run.
2. **Deploy the commit.** No environment variable changes, no new keys, no provider change.
3. **Hard-refresh the browser once** (Ctrl-Shift-R / Cmd-Shift-R). `taxonomy.js`, `categories.js`
   and `projectnet2d.js` all changed; a cached copy will show the old roster.
4. **Open any project's detail page.** What he will see, different from before:
   * The **Project Signal Network** draws **five planets, not six**. Systems and Dynamics is gone.
   * **Every planet now carries its category's name** — "Cost and EVM Performance", "Schedule
     Performance", "Cost Risk", "Document-Derived Condition Signals", "Delivery Quality
     Performance" — set below the body, wrapped to two lines where needed. The A1…A6 key stays
     inside the disc as the identifier.
   * The **Signal Flow** draws the same five categories and **27 modules**, down from 42.
   * The header strip reads **"27 MODULES IN SERVICE"** and **"5 WEIGHTED PERFORMANCE
     CATEGORIES"**. Both figures are derived; neither was typed anywhere.
   * A project status is now **Indeterminate unless all five** categories carry a posture.
     It was four before. **Expect more Indeterminate rows, not fewer.**
5. **The Governance Decision card is unchanged.** It still says what it said at Run 94. That is
   the stop, not an oversight; see section 5.

---

## 2. The fifteen retirements

**All fifteen resolved to exactly the ids the order gave. Zero mismatches, zero unresolved.**
Re-verified independently through `registry.service_index()` keyed on `module_name` (the field is
`module_name`; there is no `name` key — the Run 94 trap is real and was avoided).

| Module | Id | Resolved | Now retired | Now in service |
|---|---|---|---|---|
| Bayesian EAC | A1.3 | yes | yes | no |
| Kalman Filter SPI Smoother | A1.4 | yes | yes | no |
| CPI Shrinkage Forecast | A1.10 | yes | yes | no |
| Line of Balance | A2.2 | yes | yes | no |
| CCPM Buffer Health | A2.3 | yes | yes | no |
| Reference Class Forecasting | A3.1 | yes | yes | no |
| Analogous Estimating Ratio | A3.7 | yes | yes | no |
| Inflation Adjustment Index | A3.9 | yes | yes | no |
| Document Risk Score | A4.1 | yes | yes | no |
| Specification Conflict Density | A4.10 | yes | yes | no |
| Sensitivity Analysis | A5.2 | yes | yes | no |
| Scenario Modeling | A5.4 | yes | yes | no |
| Queueing Theory Bottleneck | A5.6 | yes | yes | no |
| Agent-Based Supply Chain | A5.7 | yes | yes | no |
| Discrete Event Simulation | A5.8 | yes | yes | no |

**The mechanism** is Run 43's, unchanged: the `RETIRED ` note prefix on the registry CSV row in
`p0-baseline/module_renumbering_map.csv`. No tombstone, no refusal, no new error class, no second
list. **Every identifier still resolves** and `registry.retired_modules()` still lists all fifteen.

**The reasons, and how the two groups were decided from the tree rather than from the order.**
The order says the reason for "the Systems and Dynamics modules and the others defined on
structures" is that no project document states the input; for the rest, that the owner removed
them from the retained roster at Run 89. "Defined on structures" was resolved against
`server/app/simulation/canonical.py:CANONICAL_STRUCTURE_KEYS`, whose members among the fifteen are
**A2.2, A2.3, A5.6, A5.7**. Together with the rest of A5 that gives:

* `RETIRED Run 95 as <id>: no project document states the input this module requires.`
  — A2.2, A2.3, A5.2, A5.4, A5.6, A5.7, A5.8
* `RETIRED Run 95 as <id>: the owner removed it from the retained roster at Run 89.`
  — A1.3, A1.4, A1.10, A3.1, A3.7, A3.9, A4.1, A4.10

A4.1's row keeps its pre-existing alias note appended after the retirement reason. The form
`RETIRED Run N as <id>: <reason>.` is Run 89's own; no new vocabulary was invented.

### The resulting counts — measured, nowhere written as a literal

| | before | after |
|---|---|---|
| registered | 101 | **101** |
| in service | 60 | **45** |
| retired | 41 | **56** |
| the server computes (`available_modules()`) | 59 | **45** |
| supplied (`in_service − computes`) | 1 | **0** |
| `unported_modules()` | `['A4.1']` | **`[]`** |

Per category in service: **A1 7 · A2 4 · A3 4 · A4 8 · A5 0 · A6 4 · B1 4 · B2 1 · B3 5 · B4 1 ·
C1 7.**

**THE FIVE WEIGHTED PERFORMANCE CATEGORIES HOLD 27, NOT 30.** The retained roster has been
described as 30 for several runs. It is 27. Nothing was retired or un-retired to close that gap,
and no target count is written anywhere in the tree. **The gap is the finding and it is reported
plainly.**

**A4.1 was the platform's only unported module and retiring it emptied the set.** `supplied` moved
1 → 0 by itself; no value was hard-coded. Run 88's note that A4.1's specification carried
`STOPPED. Not specified.` is confirmed; Run 89's replacement of B1.2's old `doc` arm is confirmed
clean — nothing traced back to A4.1.

---

## 3. Every specification moved to the archive

Run 91's convention followed exactly: `specifications/archive/<same filename>.md`, section moved
**verbatim**, one pointer line left in the live file. **No section was deleted.** Four archive
files were created (A1, A2, A3, A4); `archive/A5_system_dynamics.md` was appended to. Each new
file's header states that the convention is Run 91's and is not invented here, and repeats Run
91's note that A1.1's section was DELETED at Run 43 and is not reconstructed.

The line left in each live specification, quoted in full for one of each shape:

```
## A1.3 — Bayesian EAC — RETIRED at Run 95, not in service. Its specification is archived verbatim at `specifications/archive/A1_cost_and_evm.md`; the identifier still resolves and is still listed by `registry.retired_modules()`.
```

The same line, with the module's own name and its own archive filename, stands in place of:
**A1.4, A1.10** (`A1_cost_and_evm.md`); **A2.2, A2.3** (`A2_schedule_performance.md`);
**A3.1, A3.7, A3.9** (`A3_cost_risk.md`); **A4.10** (`A4_document_derived_signals.md`);
**A5.2, A5.4, A5.6, A5.7, A5.8** (`A5_system_dynamics.md`).

**A4.1 is the one exception and it is stated rather than smoothed over.** A4.1 never had a
specification. What existed was a `### A4.1 — Document Risk Score. STOPPED. Not specified.`
subsection under "Stopped specifications", recording that no runner existed and naming the
standing contradiction — a module in service that neither computed nor abstained but raised. That
note is archived verbatim under its own heading, and the line left in its place is:

```
### A4.1 — Document Risk Score — RETIRED at Run 95, not in service. Its stopped-specification note is archived verbatim at `specifications/archive/A4_document_derived_signals.md`; the identifier still resolves and is still listed by `registry.retired_modules()`.
```

The archived note describes the state *before* this run and was not corrected — correcting it
would make it a composition rather than a record. The archive header says so.

---

## 4. The decision card as it renders

**NOT PRODUCED. Goal four was stopped before any change was made.** The card renders today
exactly as it rendered at Run 94; this run did not touch it, so there is no new rendering to
print, and printing the old one as though it were this run's output would misrepresent the work.
Section 5 states why the stop was mandatory. **This is the part of the run the owner most wanted
and it is the part that did not happen.**

---

## 5. Per goal: reached or not, every iteration including the failures

### Goal one — the registry holds the owner's roster. **REACHED.**

* **Iteration 1 — measure.** Resolved all fifteen names through `service_index()` on
  `module_name`. All fifteen matched the order's ids exactly. Simulated the removal: 45 in
  service, A5 empties, 27 across the five. **No hypothesis needed; the premise held.**
* **Iteration 2 — change one thing.** Wrote the `RETIRED ` notes onto the fifteen CSV rows.
  Measured: 45 in service, 56 retired, `unported_modules()` empty, `supplied` 0. **Goal met.**
* **Iteration 3 — the archive.** Moved fourteen `##` sections and one `###` subsection.
  Verified bodies present in the archive and pointer lines in place. **Goal met.**

**No failed iteration on goal one.**

### Goal two — Systems and Dynamics is gone; five categories carry the weight. **REACHED.**

* **Iteration 1 — establish Run 51's empty-category rule from the tree, as §3.4 requires.**
  **THE RULE DOES NOT EXIST.** Searched `REPORT_2026-08-22_run51_delivery.md`, every `Run 51`
  citation in `server/` and `assets/`, `build_client_taxonomy.py` and `taxonomy.js`. There is no
  code path anywhere that removes a category holding no module. Run 89's report refers to "Run
  51's empty-category removal", but Run 51 delivered **derived counts**, not a removal rule.
  Before this run the generator emitted **every** category the authority declares, whatever its
  module list came to — D1 Portfolio Health has shipped as an empty container since Run 43.
  **So the rule had to be built, not followed.** Reported here rather than assumed.
* **Iteration 2 — build it, and it was WRONG.** Made the generator drop any category whose module
  list came out empty. **It removed TWO categories, not one:** A5 *and* D1 Portfolio Health.
  `test_map_and_module_count` went to 70/75 and, worse, its check that "the taxonomy genuinely has
  a portfolio-level category to exclude" — the check that stops every project-level filter below
  it from being vacuous — would have started asserting nothing. **This was my mistake, caught by
  measuring rather than by reasoning, and it is reported as a failed iteration.**
* **Iteration 3 — scope the rule.** Restricted the drop to `group == "A"`, still derived (group is
  a field on the authority row; no category is named). The tree's own precedent for an empty
  **portfolio-level** container is that it stays, and this run does not overturn a precedent it
  was not asked to touch. Measured: A5 gone, D1 restored, 11 categories emitted. **Goal met.**
* **Iteration 4 — the status architecture.** `_REQUIRED_CATEGORIES` → `("A1","A2","A3","A4","A6")`,
  `_SUPPORTING_CATEGORIES` → `()`, both still defined **once** in `compute.py` and imported by
  `spec_projection.py`. Weight profile → the owner's five. Measured below. **Goal met.**

**The measurements §3 requires:**

* **Worst-wins unchanged: 1024 combinations, 0 mismatches.** Run 89 measured 256 over four
  categories; with five required that becomes `4**5 = 1024`, and the published status equals
  `fusion.worst_band` computed independently over the same contributing categories for every one.
  The sweep is now driven by `sp.REQUIRED_CATEGORIES` itself, so it follows the architecture
  instead of restating it.
* **Indeterminate follows from any one of the five.** Dropping each of A1, A2, A3, A4, A6 in turn
  yields `status='Indeterminate'`, `official=False`, the missing category named, and the fused
  band **still recorded beside it**.
* **Weighted Voting is still comparison-only and still cannot set the status.**
  `test_run87_comparison_only.py` is **ALL PASS**, and §3.1's hard gate — *prove the test can
  still fail* — is met by its own section 4, which ran and went red on demand:
  `[PASS] NEUTRALISED: B1 now carries the adverse band: got 'Red'` and
  `[PASS] NEUTRALISED: project status flips -- this is the defect: got 'Red'`, then
  `[PASS] project status back to unchanged: got 'Green'` on restore.
* **Data Integrity is out, executably.** `WEIGHTED_VOTING_EXCLUDED_CATEGORIES = {"C1"}` and its
  assert are untouched; injecting C1 into the profile still raises. A **new** executable assert
  requires the profile to sum to 1.0. Measured: `{'A1':0.28,'A2':0.28,'A3':0.17,'A4':0.11,
  'A6':0.16}`, sum `1.0`, `C1 in profile? False`. `WEIGHT_PROVENANCE` names the owner's stated
  authority, Run 95 section 3 — his decision, not a derived or literature value.
* **The empty supporting tier is answered honestly, not left as a dead key.**
  `SUPPORTING_CATEGORIES` is `()` and `supporting_assessed` / `supporting_not_assessed` publish
  `[]`. They are kept rather than deleted because `documents._result_view`, `spec_projection` and
  the client all read them, and a key that vanishes reads as a missing field rather than as an
  empty tier. This is asserted, not assumed.

### Goal three — both charts read cleanly. **REACHED, IN A REAL BROWSER.**

* **Iteration 1 — the labels.** Every planet now draws its category's **name**. It is set **below
  the body**, not inside it: the MODEL planet radius is `17` and identical for every planet, so
  nothing fits inside at a legible size, and growing the disc would start encoding something in a
  radius that must encode nothing. `wrapLabel` breaks on spaces to at most two lines against a
  real `measureText` width. **Nothing is truncated** — a cut category name is one the reader still
  cannot resolve. The key stays inside the disc as the identifier.
* **Iteration 2 — the driver was vacuous and was fixed.** `drive_run94_charts.py` checked drawn
  moons against the **typed** set `{'A5.1','A5.5','B4.4'}`. That would have gone on passing while
  drawing all fifteen of this run's retirements. It now reads `registry.retired_modules()`.
* **Iteration 3 — render and read back.** Rendered by `drive_run94_charts.py` in Chromium at
  `/opt/pw-browsers` against a live server, **1280px and 1024px**, across the theme authority's
  themes. Read from the **rendered DOM and the scene graph**, not from source:

```
  docs=27 modules=27 categories=5
  headers: ['27 SUPPORTED DOCUMENT TYPES', '4 UPLOADED ON THIS PROJECT', '27 MODULES IN SERVICE',
            '6 WITH A CURRENT RESULT', '5 WEIGHTED PERFORMANCE CATEGORIES', '2 CARRY A POSTURE',
            'GOVERNED PROJECT STATUS', 'INDETERMINATE']
  category labels: ['A · Cost and EVM Performance', 'A · Schedule Performance', 'A · Cost Risk',
                    'A · Document-Derived Condition Signals', 'A · Delivery Quality Performance']
  RETIRED MODULE LABELS DRAWN: [] (must be empty)
  MODULE LABELS DRAWN: 27  with an identity ring: 27
  TRUNCATED LABELS (must be empty): []
  SIGNAL FLOW labels rendered: 75   OVERLAPPING LABEL PAIRS: 0
  planets=5 moons=27 sun=[{'kind':'health','key':'__health__','state':'unlit',
                           'status':'Indeterminate','x':576,'y':310,'r':42}]
  MODEL planet radii (size encodes nothing): [17]
  MODEL moon orbit radii (radius encodes nothing): [63]
  moon orbital rates (speed encodes nothing): [0.22]
  planet states: [('A4','computed','Red'), ('A3','not_called',None), ('A6','not_called',None),
                  ('A2','abstained',None), ('A1','computed','Green')]
  RETIRED MODULES THE REGISTRY DECLARES: 56
  RETIRED MOONS DRAWN: [] (must be empty)
  MOONS CARRYING AN IDENTITY COLOUR: 27 of 27; distinct rim colours drawn: 27
  PLANET NAME LABELS DRAWN: 5 for 5 planets
    PLANET A4   labelled ['Document-Derived', 'Condition Signals']
    PLANET A3   labelled ['Cost Risk']
    PLANET A6   labelled ['Delivery Quality', 'Performance']
    PLANET A2   labelled ['Schedule', 'Performance']
    PLANET A1   labelled ['Cost and EVM', 'Performance']
  PLANETS WITH NO NAME LABEL (must be empty): []
  'Systems' OR 'Dynamics' DRAWN ANYWHERE ON THIS CHART (must be empty): []
  STREAM Cost and EVM Performance            terminates=at-centre  arrowhead=yes
  STREAM Schedule Performance                terminates=short      arrowhead=no
  STREAM Cost Risk                           terminates=short      arrowhead=no
  STREAM Document-Derived Condition Signals  terminates=at-centre  arrowhead=yes
  STREAM Delivery Quality Performance        terminates=short      arrowhead=no
  MAX OVERLAPS ACROSS ALL RENDERS: 0
```

  Every standing chart rule holds and was measured, not argued: only the weighted performance
  categories render; the three MODEL values are **one value each**, so nothing is encoded in size,
  distance or speed; a category with no posture terminates **short, blunt, no arrowhead**; the sun
  is **unlit** on Indeterminate; the population was never shrunk to fit — 27 modules and 75 flow
  labels were drawn with **0 overlapping pairs**.

  **The counts followed automatically. Not one of them was edited.** `LIN_TAXONOMY_COUNTS` is now
  `{registered:101, inService:45, retired:56, serverComputes:45, supplied:0}`, regenerated by
  `build_client_taxonomy.py`; `--check` reports *"both client artifacts are exactly what the
  authorities generate"*. Neither `taxonomy.js` nor `categories.js` was hand-edited.

  **`performanceCategories()`'s `!== 6` was neither retyped as 5 nor deleted.** Either would repeat
  the failure it was written against — a stated number that drifts from the computed one. It now
  warns when a category is about to be **drawn with nothing in it**, which is the rule that
  actually matters and needs no number, and is the client-side half of the generator's new rule.

  **`neural_flow.js`'s dead fallback roster is confirmed still unreachable.** `FB_CATS` and
  `RAW_MODS` each appear exactly twice in the file: their declaration and one comment. Nothing
  references them. They were not resurrected and not deleted.

### Goal four — the Governance Decision card states a finding. **NOT REACHED. STOPPED UNDER §7.7.**

**This is a mandatory stop, taken before any edit, not a failure to finish.**

**The sequence-bearing tuple, declared explicitly as required, and established from the tree —
`server/tools/participant_packages.py:1079`, `V18_TO_V19_SEQUENCE_EXCEPTION`:**

```
("assets/js/decision-ui.js", "assets/js/decision.js", "assets/js/deepdive.js",
 "assets/js/workspace.js", "assets/questionnaires/debrief.json",
 "assets/questionnaires/intake.json")
```

**Sequence-bearing files this run moved: `()` — none.**

**Note a briefing error:** I was given a five-file tuple omitting `assets/js/deepdive.js`. The
tree says **six**, and `deepdive.js` is one of them. Corrected here.

**Why the card cannot be built without moving one.** Every part of §5 lands in
`assets/js/decision-ui.js`: `renderPackage` (measured at line 587 onward) is where "Recommended
action" is emitted and where the twelve playbook blocks, the relabelling to "Decision-support
finding" / "Material drivers" / "Evidence used" / "Assessment limitations", and the hide-rather-
than-print rules would have to live; `renderDecideForm` (line ~604 onward) is where the
disposition vocabulary is rendered and where §5's five dispositions would have to reconcile
against the eight in `research_decision.DISPOSITIONS`; and the "Deciding authority" input at line
651 is the very text §5.1 orders removed. **There is no route to §5 that leaves that file
byte-identical.** §7.7 orders me to stop and report before proceeding, and the order's own
briefing predicted this exact collision. I stopped.

**What was measured about the card before stopping, so the next run does not start blind:**

* `assets/js/decision-ui.js:587` — `html += field("Recommended action", pkg.recommended_action);`
  is the label §5 renames. The card's fields are `Recommended action`, `Detected condition`,
  `Alternatives considered`, `Uncertainty`, `Limitations`, `Where this applies`,
  `When this expires`, `Supporting evidence and provenance`.
* `field()` **already hides** a null/undefined/empty value and prints nothing. So §5.4's "hide
  rather than print 'not established'" is closer than the order assumes: what produces the empty
  card is that `pkg.*` carries nothing, not that the renderer prints placeholders.
* **§5.2's block will essentially always hide, and this is confirmed.**
  `recommendation_options.js:149` returns `available: false` on every current row, and **B4.4
  What-If Scenario Matrix is retired** — Run 89 retired it and it is still retired. The
  alternatives block therefore has no computing source at all.
* The card's controls, opened, are `['Record decision','Export audit JSON','Export Report
  (XLSX)']` with a required reviewer-rationale field (Run 92's measurement; not re-measured here).

**The two playbook/tree conflicts §5 pre-resolved were both confirmed real:** the playbook
configures six required categories where the owner's ruling is five, and its module-response
tables name DSM Rework Propagation and Rework Feedback Loop, **both of which are retired** (A5.1
and A5.5, Run 89) — and I confirm the Run 88 naming trap: `DSM_Rework_Propagation` is the
**retired** method class, `DSM_Rework_Cat5` is the current one. **No third conflict was reached**,
because the build stopped before the playbook was applied.

---

## 6. Every premise in this order, and in the briefing, that proved false against the tree

1. **§3.4's premise that a Run 51 empty-category rule exists. FALSE.** There is no such rule
   anywhere in the tree — no code, no check, nothing in the Run 51 report. The generator emitted
   every declared category regardless. The rule was **built by this run**, derived and scoped to
   group A, and that is said plainly rather than presented as following a precedent.
2. **The briefing's `test_run6_known_answer` baseline of 488/489. FALSE.** Measured at `385cdb7`
   by checking the commit out in the real repository: **487/489**. Both failures are pre-existing
   and both are pinned-baseline drift checks. This run added none.
3. **The briefing's five-file sequence-bearing tuple. INCOMPLETE.** The tree says six;
   `assets/js/deepdive.js` was missing from the briefing.
4. **The briefing's prediction that the behaviour digest would move. FALSE.** Measured
   `3839c63df2a02099e2d536defb4b89e07d55d1eeb5ac6fee31704f2307eacdb7` — **unchanged**, and this is
   correct rather than a fault. `behaviour_digest()` executes each of the 100 **scientific
   targets** through its own runner. Retiring a module does not delete its runner and does not
   change any module's arithmetic; Run 95 changed the **roster** and the **rollup**, not what any
   module computes. **So B15 would not have fired on this run** had the gate reached it. The
   digest's `simulation_version` field does read `sim-2026.08-v44`.
5. **The briefing's claim that the driver crashes on `no such table: participants` from a plain
   copy of `dev.db`. PARTLY FALSE.** The driver seeds its own `ResearchAdmin` row. What it needs
   is the database **migrated to head** — `python -m alembic upgrade head` against the scratchpad
   copy, which applied `0030 → 0031`. After that a plain copy is sufficient.
6. **The order's §2 arithmetic and §3's "A5 holds no module in service after goal one". BOTH
   TRUE**, re-verified independently rather than trusted.

---

## 7. Real versus harness measurements; suites not run

**Real (browser, live server, rendered DOM and scene graph):** everything in section 5's goal
three block. Chromium at `/opt/pw-browsers`, `PLAYWRIGHT_BROWSERS_PATH` set, `playwright install`
**not** run. 1280px and 1024px.

**Real (production code paths, no browser):** the registry counts, the 1024-combination worst-wins
sweep, the Indeterminate gate, the weight profile and its two executable asserts, the client
taxonomy `--check`, the behaviour digest, the freeze-gate refusal.

**HARNESS, and stated as such:** the fixture row `01M11XEYX5V5S6CQSCSJBHBV6T`-equivalent that the
driver builds. Its readings for A2, A4, A5, B1, B2 are **fixtures written by `store_reading`** on
Run 81's precedent — they are not model behaviour. There is **no `ANTHROPIC_API_KEY`**; extraction
ran through `StubExtractor` throughout. Database was a **scratchpad SQLite copy** of
`server/dev.db` migrated to head. **`DATABASE_URL` was never pointed at production Postgres.**

**Suites run, with results:**

| suite | result | note |
|---|---|---|
| `test_run87_comparison_only` | **ALL PASS** | §3.1 gate; section-4 red proof exercised |
| `test_run89_required_core` | **ALL PASS** | rewritten for the five |
| `test_run89_data_integrity_gate` | **ALL PASS** | weights asserted by value now |
| `test_run17_scientific_methods` | **287/287** | was 287/287 |
| `test_map_and_module_count` | **77/77** | was 75/75 |
| `test_group_assignment` | **18/18** | was 16/16 |
| `test_run24_empty_project_diagram` | **55/55** | was 53/53 |
| `test_run26_counts_and_wiring` | **56/57** | pre-existing emission-block digest failure only |
| `test_run30_non_vacuity` | **120/120** | unchanged |
| `test_run94b_charts` | **8/8** | a retirement removed its colours by itself |
| `test_run16_final_flow_and_rail` | **74/74** | unchanged |
| `test_run23_signal_flow_truthfulness` | **37/37** | unchanged |
| `test_run25_rail_removal` | **56/56** | unchanged |
| `test_run63_four_charts` | **20/24** | pre-existing baseline, unmoved |
| `test_run6_known_answer` | **487/489** | pre-existing baseline, unmoved |
| `node test_run89_indeterminate_brief.js` | **ALL PASS** (15/15) | unchanged |
| `test_run37_freeze_gate` | **16/19** | the CANDIDATE refusal, pre-existing |

**Suites NOT run, and I say so rather than implying coverage:** `test_documents_b7b`,
`test_run3_adapter`, `test_run10_state_protection`, `test_workspace_t3t5`,
`test_run48_current_period`, `test_run22_production_tree_completeness`,
`test_run28_participant_packages`, `test_run38_frozen_immutability`,
`test_run39_frozen_immutability`, and the full `run_all_suites.sh`. Several of these are known
pre-existing failures; **none was chased and none was silently absorbed.** Their state at the end
of this run is **not known** and is not claimed.

---

## 8. Found and not fixed

1. **`spec_apply.PASS_ONE` still lists A5.** `("A1","A2","A3","A4","A5","A6","C1")` in
   `server/app/simulation/spec_apply.py:84`, and `has_specification("A5")` is still `True` because
   the live A5 file survives as pointer lines. **Measured to be inert:** the stored
   `category_statuses` on a freshly computed row contains no A5 entry, and both charts drew five
   categories. It is a dispatch slot that can never produce a reading. Not fixed because
   `server/app/simulation/` holds Run 79's wiring, which §7.3 protects, and because changing it
   would move behaviour beyond what was ordered. **A category that can never report is still
   listed as one the platform tries to report on, and a future run should decide it.**
2. **The Signal Flow header reads "27 MODULES IN SERVICE"** where the registry's in-service figure
   is **45**. The 27 is the charted population — the five weighted categories only — and it is
   correctly derived from that population. The **word** is imprecise, the way Run 51 found
   "registered" imprecise on the same strip. Pre-existing wording, not introduced here, not fixed.
3. **The `test_run26_counts_and_wiring` emission-block digest** (`committed 4b35f4ed5b8a vs
   regenerated 04c4818b1185`) is pre-existing and unrelated to this run. Not chased.
4. **`registry._retired_reason(mid)` still raises `AttributeError`** when passed a module id
   instead of a row. Run 91 found it; still not fixed; not touched.
5. **The retained roster is 27, not the 30 that has been described for several runs.** Not
   reconciled, per §2. This is the run's headline finding after the retirements themselves.

---

## 9. Every guarantee, marked

**VERIFIED by measurement:**

* The fifteen are out of service; all fifteen identifiers still resolve; `retired_modules()` lists
  all fifteen. *(measured)*
* Every rendered count follows automatically with no further edit; `build_client_taxonomy.py
  --check` is green. *(measured)*
* No target module count is written as a literal anywhere. Four checks that carried such literals
  now assert against the server registry read independently — **strictly stronger**, not weaker.
  *(measured)*
* No specification section was deleted; all fifteen are archived verbatim. *(measured)*
* No retired module is drawn, and Systems and Dynamics is drawn nowhere — asserted on **drawn**
  moon keys, drawn planet keys and drawn label text, against `registry.retired_modules()` as the
  oracle. *(measured in a real browser)*
* Data Integrity is not in the weighted profile and cannot reach the project status; the
  executable assert still fires when injected. *(measured)*
* Weighted Voting remains comparison-only and cannot set or alter the official status; the test
  proving it **can still fail** was exercised and went red on demand. *(measured)*
* Worst-wins is unchanged across all 1024 combinations; Indeterminate follows from each of the
  five. *(measured)*
* The three Run 70 recommendation checks are untouched and unweakened; the Indeterminate brief is
  ALL PASS. *(measured)*
* Run 94b's palette generator is untouched and a retirement removed colours by itself. *(measured)*
* No sequence-bearing file was moved: `()`. *(measured, `git status` and the diff)*
* `DATABASE_URL` never pointed at production. No `git add -A` or `git add .` was used; every add
  was explicit; `git status --porcelain` was run before every commit. No key value was printed,
  logged or committed. *(verified)*

**NOT MET, stated plainly rather than argued around:**

* **The decision card was not built and not rendered.** Goal four is not met. It was stopped under
  §7.7 before any change, which is what the order requires, but the goal is **not met** and I am
  not going to dress the stop up as a pass.
* **The freeze gate is BLOCKED.** It refused at the CANDIDATE fixed point before evaluating any
  blocker — `CANDIDATE as set` `eba2a11d…` vs `as computed` `c8a9873c…`. **That refusal is the
  mechanism working.** The constant was **not** set, per §6.3 and §8.10. Work is committed to
  `main` and **not pushed and not merged**.

**COULD NOT BE VERIFIED, and I say so rather than reasoning it out:**

* Whether the suites listed as not-run in section 7 are still at their pre-existing figures. They
  were not run. I do not know.
* Whether B15 would fire. The gate never reached it. The behaviour digest is unmoved, which
  **suggests** it would not, but the gate refused first and that is a measurement I do not have.
